background {
    monitor =
    color = rgba(7, 12, 18, 1.0)
}

input-field {
    monitor =
    size = 300, 40
    outline_thickness = 1
    outer_color = rgb(1C3347)
    inner_color = rgb(0A1520)
    font_color = rgb(7EB8C9)
    fade_on_empty = false
    placeholder_text = <span>PASSWORD</span>
    hide_input = false
    position = 0, -80
    halign = center
    valign = center
}

label {
    monitor =
    text = SIGINT-01 // AUTHENTICATION REQUIRED
    color = rgba(78, 184, 201, 0.8)
    font_size = 14
    font_family = JetBrains Mono Nerd Font
    position = 0, 80
    halign = center
    valign = center
}

label {
    monitor =
    text = $TIME
    color = rgba(74, 122, 144, 0.8)
    font_size = 24
    font_family = JetBrains Mono Nerd Font
    position = 0, 160
    halign = center
    valign = center
}

label {
    monitor =
    text = WARNING: This system is actively monitored. Authorized use only.
    color = rgba(42, 96, 128, 0.7)
    font_size = 11
    font_family = JetBrains Mono Nerd Font
    position = 0, -160
    halign = center
    valign = center
}

label {
    monitor =
    text = Unauthorized access or use is prohibited and will be prosecuted to the full extent of applicable law.
    color = rgba(28, 52, 71, 0.8)
    font_size = 10
    font_family = JetBrains Mono Nerd Font
    position = 0, -190
    halign = center
    valign = center
}
